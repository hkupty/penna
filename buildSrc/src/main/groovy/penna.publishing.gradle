plugins {
    id 'maven-publish'
    id 'signing'
}

java {
    withJavadocJar()
    withSourcesJar()
}

jar {
    manifest {
        attributes 'Implementation-Version': version,
                   'Implementation-Title': project.name
    }
}

